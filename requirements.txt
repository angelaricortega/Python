# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Dependencias del proyecto encuesta-api
#
# Entorno recomendado: Python 3.11+
# Instalación: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Framework web ASGI — núcleo de la API
fastapi>=0.110.0

# Servidor ASGI para desarrollo y producción
uvicorn[standard]>=0.29.0

# Validación de datos — v2 (campo polimórfico, validators, model_config)
pydantic>=2.6.0

# ─── Dependencias de desarrollo y pruebas ────────────────────────────────────

# Framework de pruebas unitarias e integración
pytest>=8.1.0

# Cliente HTTP para TestClient de FastAPI (pruebas de integración)
httpx>=0.27.0

# ─── Dependencias para bonus ─────────────────────────────────────────────────

# Análisis de datos (script cliente CSV + estadísticas bonus)
pandas>=2.2.0

# Cliente HTTP para script consumidor de la API (bonus)
requests>=2.31.0
